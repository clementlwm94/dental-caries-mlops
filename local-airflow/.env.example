# MLflow Configuration
MLFLOW_TRACKING_URI=https://mlflow-server-{project-number}.us-central1.run.app
MLFLOW_GCS_ARTIFACT_ROOT=gs://{bucket-name}/mlflow-artifacts/

# Google Cloud Configuration - credentials handled via volume mount

# Airflow Configuration
AIRFLOW_UID=31234
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin

# Evidently Configuration (Optional)
EVIDENTLY_TOKEN=your-evidently-token
EVIDENTLY_ORG_ID=your-evidently-org-id

# Airflow Fernet Key (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
AIRFLOW_FERNET_KEY=your-fernet-key-here
